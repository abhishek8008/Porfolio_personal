# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=your_neon_postgres_connection_string

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRES_IN=7d

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ===========================================
# Redis Configuration (for caching - OPTIONAL)
# ===========================================
# The app works without Redis, but caching improves performance

# Option 1: Redis URL (for cloud services like Upstash, Redis Labs, Railway)
# REDIS_URL=redis://default:password@hostname:port

# Option 2: Individual Redis settings (for local Redis)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0
# REDIS_TLS=false

# ===========================================
# FREE REDIS OPTIONS:
# ===========================================
# 1. Upstash (Recommended - Free tier available)
#    - Sign up at: https://upstash.com
#    - Get your REDIS_URL from the dashboard
#    - Free tier: 10,000 commands/day, 256MB storage
#
# 2. Redis Labs (Free tier available)
#    - Sign up at: https://redis.com/try-free/
#    - Free tier: 30MB, shared cluster
#
# 3. Railway (Free tier with usage limits)
#    - Sign up at: https://railway.app
#    - Add Redis service to your project
#
# 4. Local Redis (for development)
#    - Install Redis locally
#    - Use REDIS_HOST=localhost, REDIS_PORT=6379
